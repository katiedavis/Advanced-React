# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String
    desription: String
    Price: Int
    image: String
    largeImage: String
  ): Item!
}

type Query {
  items: [Item]!
}
